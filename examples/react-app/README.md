# React Example Placeholder

This folder is reserved for a future runnable React example app.

Current package usage:

```tsx
import { FhiRateCard } from '@fhi/rate-card-react';
import '@fhi/rate-card-core/src/components/rate-card/styles/rate-card.css';

<FhiRateCard data={planJson} onCtaClick={(plan) => console.log(plan)} />;
```
