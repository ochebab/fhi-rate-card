# Angular Example Placeholder

This folder is reserved for a future runnable Angular example app.

Current package usage:

```ts
import { FhiRateCardModule } from '@fhi/rate-card-angular';
```

```html
<fhi-rate-card [data]="planJson" (ctaClick)="onCtaClick($event)"></fhi-rate-card>
```
